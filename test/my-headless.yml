apiVersion: v1
kind: Service
metadata:
  labels:
    app: my-headless 
  name: my-headless
spec:
  clusterIP: None
  ports:
  - name: my-headless
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: my-statefulset
  type: ClusterIP
status:
  loadBalancer: {}
