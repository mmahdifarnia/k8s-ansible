apiVersion: v1
kind: Service
metadata:
  name: my-nodeport
  labels:
    app: my-nodeport
spec:
  type: NodePort
  ports:
  - name: 80-80
    nodePort: 30000
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: my-deployment
