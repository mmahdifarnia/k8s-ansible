apiVersion: v1
kind: Service
metadata:
  labels:
    app: my-clusterip
  name: my-clusterip
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: my-deployment
  type: ClusterIP
status:
  loadBalancer: {}
