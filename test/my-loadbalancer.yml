apiVersion: v1
kind: Service
metadata:
  labels:
    app: my-loadbalancer
  name: my-loadbalancer
spec:
  ports:
  - name: my-loadbalancer
    nodePort: 30000
    port: 8080
    protocol: TCP
    targetPort: 80
  selector:
    app: my-deployment
  type: LoadBalancer
status:
  loadBalancer: {}