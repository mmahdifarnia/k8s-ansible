apiVersion: v1
kind: Pod
metadata:
  name: kafka-test-client
  namespace: kafka
spec:
  containers:
  - name: kafka-test-client
    image: confluentinc/cp-kafka:7.4.0
    command:
      - sh
      - -c
      - "exec tail -f /dev/null"