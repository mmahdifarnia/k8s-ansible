apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-app2
  namespace: default
  labels:
    app: nginx-app2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-app2
  template:
    metadata:
      labels:
        app: nginx-app2
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        volumeMounts:
        - name: nginx-config
          mountPath: /etc/nginx/conf.d/
        - name: html-content
          mountPath: /usr/share/nginx/html/
      volumes:
      - name: nginx-config
        configMap:
          name: nginx-app2-config
      - name: html-content
        configMap:
          name: nginx-app2-html
