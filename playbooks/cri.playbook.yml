---
- name: Install and configure containerd
  hosts: all
  become: true
  tasks:
    - name: Import role for Installing and configuring containerd
      ansible.builtin.include_role:
        name: k8s
        tasks_from: cri/install.yml
